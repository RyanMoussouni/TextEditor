package MainGUI;

import javax.swing.*;
import java.awt.*;

public class MyFrame extends JFrame {
    public MyFrame(Dimension dimension) {
        super();
        setSize(dimension);
        setLayout(new BorderLayout());
    }
}